# ADC Strategy (Locked)

Chaincraft uses Strategy A: External ADC as Standard.

All analog controls are read via a dedicated external ADC.
This decouples the control surface from DSP constraints and enables future expansion.

This decision is locked for production.
